{
  "lookup": {
    "group": "join-functions",
    "name": "lookup",
    "description": "Returns a record from a subquery (the lookup table) producing a match between a field in the source table (sourceField) and a field in the lookup table (lookupField).",
    "syntax": [
      "lookup(lookupTable, lookupField [, sourceField] [, executionOrder])"
    ],
    "parameters": [
      {
        "name": "lookupTable",
        "type": [
          "execution block"
        ],
        "description": "Subquery producing the lookup table.",
        "required": true,
        "repetitive": false
      },
      {
        "name": "sourceField",
        "type": [
          "expression"
        ],
        "description": "The matching field in the source table (left side).",
        "required": false,
        "repetitive": false
      },
      {
        "name": "lookupField",
        "type": [
          "expression"
        ],
        "description": "The matching field in the lookup table (right side).",
        "required": true,
        "repetitive": false
      },
      {
        "name": "executionOrder",
        "type": [
          ""
        ],
        "description": "Defines which side of the join will be executed first. The allowed values are auto, leftFirst, rightFirst. The default value is auto.",
        "required": false,
        "repetitive": false
      }
    ],
    "examples": [],
    "returns": [
      "record"
    ],
    "longDescription": "In a case, where more than one match is produced in the lookup table, only the first matching record of the lookup table is returned. When the key values in the source and lookup table are both null, corresponding records aren't matched.",
    "aliases": [
      "lookup"
    ]
  },
  "getNodeName": {
    "group": "join-functions",
    "name": "getNodeName",
    "description": "Returns the Smartscape node name.",
    "syntax": [
      "getNodeName(expression)"
    ],
    "parameters": [
      {
        "name": "expression",
        "type": [
          "string"
        ],
        "description": "The expression to determine the Smartscape node ID.",
        "required": true,
        "repetitive": false
      }
    ],
    "examples": [],
    "returns": [
      "string"
    ],
    "aliases": [
      "getNodeName"
    ]
  },
  "getNodeField": {
    "group": "join-functions",
    "name": "getNodeField",
    "description": "Returns the field value for a Smartscape node.",
    "longDescription": "Returns the field value for a Smartscape node.",
    "syntax": [
      "getNodeField(expression, name)"
    ],
    "parameters": [
      {
        "name": "expression",
        "type": [
          "string"
        ],
        "description": "The expression to determine the Smartscape node ID.",
        "required": true,
        "repetitive": false
      },
      {
        "name": "name",
        "type": [
          "string"
        ],
        "description": "The Smartscape field name to be queried.",
        "required": true,
        "repetitive": false
      }
    ],
    "examples": [],
    "returns": [
      "boolean",
      "long",
      "double",
      "timestamp",
      "timeframe",
      "duration",
      "string",
      "ip",
      "uid",
      "binary",
      "array",
      "record"
    ],
    "aliases": [
      "getNodeField"
    ]
  }
}