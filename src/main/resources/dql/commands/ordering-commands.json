{
  "limit": {
    "description": "Limits the number of returned records.",
    "name": "limit",
    "type": "ordering-commands",
    "syntax": [
      "limit numberOfQueryRecords"
    ],
    "parameters": [
      {
        "name": "numberOfQueryRecords",
        "description": "If you do not specify the limit, the query renders 1000 results (this limit, by default, is appended as the last line of the query).",
        "nameAllowed": false,
        "required": true,
        "constant": true,
        "type": [
          "positive long"
        ],
        "defaultValue": 1000
      }
    ]
  },
  "sort": {
    "name": "sort",
    "description": "Sorts the records.",
    "type": "ordering-commands",
    "syntax": [
      "sort fieldname [asc | desc][, ...]"
    ],
    "parameters": [
      {
        "name": "fieldsList",
        "nameAllowed": false,
        "description": "The list of fields used for the sorting operation",
        "required": true,
        "repetitive": true,
        "type": [
          "any",
          "sorting expression"
        ]
      }
    ]
  }
}